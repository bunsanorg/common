#include "bunsan/process/detail/wait.hpp"

int bunsan::process::detail::wait(boost::process::child &&child, const std::chrono::seconds &timeout)
{
#warning TODO unimplemented, this is stub
	return wait(child);
}

